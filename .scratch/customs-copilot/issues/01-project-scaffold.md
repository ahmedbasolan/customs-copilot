# 01: Project scaffold

**What to build:** Streamlit app skeleton with dependencies and API key sidebar input. The app should run locally and show a sidebar with an OpenAI API key text input.

**Blocked by:** None (can start immediately)

**Status:** ready-for-agent

- [ ] Initialize project with `requirements.txt` (streamlit, openai, pydantic)
- [ ] Create `app.py` with basic Streamlit layout
- [ ] Add sidebar with OpenAI API key text input (session-only, never stored)
- [ ] Add page title and description
- [ ] Verify app runs locally with `streamlit run app.py`
